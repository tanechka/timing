/*
  Чтобы любой код react-redux работал,
  вам надо сказать вашему приложению,
  чтобы оно использовало react-redux,
  это делается с помощью компонента <Provider />

  Создает Redux хранилище которое хранит полное дерево состояния вашего приложения.
  Оно должно быть единственным хранилищем в вашем приложении.

  редукторы (reducers) — это функции,
  которые вы пишете для обработки отправленных действий.

  Редуктор принимает текущее состояние как аргумент и может модифицировать состояние,
  только вернув новое состояние.

  разделение на "компоненты" и "контейнеры", иначе называемые: "глупые" и "умные" компоненты

  Любой компонент Reactjs получает props на входе,
  это могут быть actions,
  state или еще что-нибудь из вашего окружения.
  Вот эти props мы и будем типизировать,
  чтобы сделать поведение компонента более предсказуемым.

  // <AddTodo />
  // <VisibleTodoList />
  // <Footer />
 */
